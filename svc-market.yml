apiVersion: v1
kind: Service
metadata:
  name: sv-market
  namespace: dev-namespace
spec:
  type: LoadBalancer
  selector:
    app: html-glass
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP