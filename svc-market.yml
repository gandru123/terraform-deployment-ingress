apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: dev-namespace
spec:
  type: LoadBalancer
  selector:
    app: html
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP